*Problems Associated with Array Data Structure
	Fixed Size problems
	Costly Insertion problems
	Costly Deletion problems
	Complex implementation of other data structures using Arrays(queues)

#linkedllist
   1.like arrays it's linear data structure it stores items in sequential manner one after the other
   2.But unlike arrays elements do not have to be in contiguous location
   3.every node stores  pointer to the next node
	 _____________     _____________     _____________
 Head-->|value|*next--|-->|value|*next--|-->|value|*next--|-->NULL
	```````````````   ```````````````   ```````````````

